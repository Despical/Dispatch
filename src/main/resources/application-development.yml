spring:
  thymeleaf:
    cache: false
  devtools:
    livereload:
      enabled: true

management:
  endpoint:
    health:
      group:
        readiness:
          include: readinessState,db,mailSync,outbox
